#include <iostream>
#include <cstdio>
#include <vector>

using namespace std;

typedef long long ll;
typedef vector<ll> vi;

struct SegmentNode {
    ll L, R;

    ll 
}

int main()
{
    ll c;
    cin >> c;

    for (ll caseNum = 1; caseNum <= c; ++caseNum) {
        ll n;
        cin >> n;

        SegmentTree
    }
}
