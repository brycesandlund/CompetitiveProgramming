#include <isotream>
#include <vector>

using namespace std;

typedef long long ll;
typedef vector<bool> vb;

int main() {
    ll k;
    for (ll cs = 1; cin >> k; ++cs) {
        
    }
    return 0;
}
